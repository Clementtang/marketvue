# MarketVue 專案開發指南

> MarketVue 專案特定的開發規範和注意事項

## 專案基本資訊

### 專案概覽
- **專案名稱**：MarketVue
- **描述**：即時多市場股票追蹤儀表板 / Real-time Multi-Market Stock Dashboard
- **當前版本**：v1.3.1
- **授權**：MIT License
- **儲存庫**：https://github.com/Clementtang/marketvue.git
- **作者**：MarketVue Contributors

### 專案路徑
```
本地開發路徑: /Users/clementtang/stock-dashboard
```

**重要**：所有檔案操作都應該使用這個路徑，不要使用 `/tmp/` 或其他臨時路徑。

## 技術棧

### 前端技術
- **框架**：React 19.1.1
- **語言**：TypeScript
- **建構工具**：Vite 7.1.7
- **樣式**：Tailwind CSS 4.1.15
- **圖表**：Recharts 3.3.0
- **圖示**：lucide-react 0.546.0
- **HTTP 客戶端**：axios 1.12.2
- **日期處理**：date-fns 4.1.0
- **網格佈局**：react-grid-layout 1.5.2
- **分析工具**：@vercel/analytics, @vercel/speed-insights

### 後端技術
- **框架**：Flask 3.0.0
- **語言**：Python 3.11.0
- **股票數據**：yfinance >=0.2.66
- **跨域**：Flask-CORS 4.0.0
- **快取**：Flask-Caching 2.1.0
- **限流**：Flask-Limiter 3.5.0
- **驗證**：marshmallow 3.20.1
- **生產伺服器**：gunicorn 21.2.0

### 部署平台

#### 前端 - Vercel
- **URL**：https://marketvue.vercel.app
- **自動部署**：main 分支 push 後自動部署
- **建構指令**：`npm run build`
- **輸出目錄**：`dist/`

#### 後端 - Render (Free Tier)
- **URL**：https://marketvue-api.onrender.com
- **地區**：Singapore
- **建構指令**：`cd backend && pip install -r requirements.txt`
- **啟動指令**：`cd backend && gunicorn --bind 0.0.0.0:$PORT app:app`
- **健康檢查**：`/api/health`
- **重要特性**：
  - ⚠️ 閒置 15 分鐘後進入睡眠
  - ⚠️ 冷啟動需要 30-60 秒
  - ⚠️ 前端已實作智能重試機制處理冷啟動

## 專案結構

```
/Users/clementtang/stock-dashboard/
├── backend/                          # Flask 後端
│   ├── app.py                        # Flask 主應用
│   ├── config.py                     # 設定檔
│   ├── requirements.txt              # Python 依賴
│   ├── data/
│   │   └── company_names.json        # 36+ 公司名稱對照表
│   ├── routes/
│   │   └── stock_routes.py           # API 路由
│   ├── services/
│   │   └── stock_service.py          # yfinance 整合
│   ├── schemas/                      # 資料驗證 schemas
│   └── utils/                        # 工具函式
│
├── src/                              # React 前端
│   ├── components/
│   │   ├── StockCard.tsx             # 股票卡片（含重試邏輯）
│   │   ├── StockManager.tsx          # 股票管理（最多 18 檔）
│   │   ├── DashboardGrid.tsx         # 儀表板網格（6x3）
│   │   ├── TimeRangeSelector.tsx     # 時間範圍選擇器
│   │   ├── ThemeSettings.tsx         # 主題設定
│   │   ├── ColorThemeSelector.tsx    # 顏色主題選擇器
│   │   ├── Footer.tsx                # 頁尾
│   │   └── NotificationBanner.tsx    # 通知橫幅
│   ├── i18n/
│   │   └── translations.ts           # 雙語翻譯（92+ keys）
│   ├── App.tsx                       # 根元件
│   └── main.tsx                      # 入口點
│
├── docs/                             # 文件
│   ├── API.md                        # API 文件
│   ├── ARCHITECTURE.md               # 架構文件
│   └── DEPLOYMENT.md                 # 部署指南
│
├── .claude/                          # Claude 助理指南
│   ├── character.md                  # 芙莉蓮角色設定
│   ├── general-principles.md         # 通用開發原則
│   ├── marketvue-guidelines.md       # 本檔案
│   └── workflows.md                  # 協作流程
│
├── .todo/                            # 內部 TODO 追蹤（不進 Git）
│   ├── detailed-tasks.md             # 6 維度任務分類
│   ├── work-logs/                    # 每日工作記錄
│   └── research-reports/             # 技術研究報告
│
├── CHANGELOG.md                      # 版本變更記錄
├── CONTRIBUTING.md                   # 貢獻指南
├── README.md                         # 中文 README
├── README_EN.md                      # 英文 README
├── ROADMAP.md                        # 公開發展藍圖
├── package.json                      # 專案元資料
└── render.yaml                       # Render 部署設定
```

## 核心功能

### 股票追蹤
- **最多 18 檔股票**：使用者可同時追蹤最多 18 檔股票
- **多市場支援**：
  - 台灣上市（.TW）：2330.TW（台積電）
  - 台灣上櫃（.TWO）：5904.TWO（寶雅）
  - 美國股市：AAPL（蘋果）
  - 香港股市（.HK）：0700.HK（騰訊）
  - 日本股市（.JP）：9983.JP（UNIQLO）

### 技術指標
- MA20（20 日移動平均線）
- MA60（60 日移動平均線）
- 即時價格變化追蹤
- 平均成交量分析

### 自訂功能
- **顏色主題**：Western（紅漲綠跌）/ Eastern（綠漲紅跌）
- **顯示模式**：深色模式 / 淺色模式 / 系統自動
- **語言切換**：繁體中文 / English
- **時間範圍**：1W, 1M, 3M, 6M, 1Y 或自訂日期

### 本地儲存
- 追蹤股票列表自動保存
- 使用者偏好設定記憶（主題、語言、顏色）

## 雙語支援要求

### 語言設定
- **主要語言**：繁體中文（zh-TW）、美式英文（en-US）
- **Translation 檔案**：`src/i18n/translations.ts`
- **目前有 92+ 個翻譯 keys**

### 雙語規則

#### ✅ 必須雙語
1. **所有 UI 文字**：按鈕、標籤、標題、提示
2. **錯誤訊息**：所有使用者可見的錯誤訊息
3. **通知訊息**：警告、成功、資訊訊息
4. **README 文件**：`README.md` (中文) + `README_EN.md` (英文)
5. **CHANGELOG**：主要用英文，但可加入中文說明

#### ❌ 不需雙語
1. **程式碼註解**：主要用英文
2. **Git commit 訊息**：僅用英文
3. **內部 TODO 文件**：用繁體中文
4. **開發者文件**：CONTRIBUTING、ARCHITECTURE（可選）

### 新增翻譯的流程

**步驟 1**：在 `src/i18n/translations.ts` 新增 key
```typescript
export interface Translations {
  // ... 現有的 keys
  newFeatureName: string;       // 新增
  newFeatureDescription: string; // 新增
}

export const translations: Record<Language, Translations> = {
  'zh-TW': {
    // ... 現有的翻譯
    newFeatureName: '新功能名稱',
    newFeatureDescription: '新功能的詳細描述',
  },
  'en-US': {
    // ... existing translations
    newFeatureName: 'New Feature Name',
    newFeatureDescription: 'Detailed description of the new feature',
  },
};
```

**步驟 2**：在元件中使用
```typescript
const translations = useTranslation(language);

return (
  <div>
    <h3>{translations.newFeatureName}</h3>
    <p>{translations.newFeatureDescription}</p>
  </div>
);
```

### 公司名稱對照
- **對照表位置**：`backend/data/company_names.json`
- **目前支援**：36+ 家知名公司
- **格式**：
  ```json
  {
    "2330.TW": {
      "zh-TW": "台積電",
      "en-US": "TSMC"
    }
  }
  ```
- **未在對照表的股票**：自動顯示 Yahoo Finance 提供的英文名稱

## API 相關

### 環境變數

#### 前端 `.env`
```bash
VITE_API_URL=http://localhost:5001
# 注意：不要加 /api 後綴
```

#### 後端 `backend/.env`
```bash
PORT=5001
PYTHON_VERSION=3.11.0
```

### API 快取策略
- **快取類型**：SimpleCache（記憶體快取）
- **快取時間**：5 分鐘
- **快取的端點**：
  - `/api/stock-data/<symbol>`
  - `/api/batch-stocks`
- **效能提升**：634x faster（1.92s → 0.003s）

### 錯誤處理與重試邏輯

#### 503 錯誤（冷啟動）
```typescript
// StockCard.tsx 中的智能重試邏輯
if (statusCode === 503) {
  // Render Free tier 冷啟動專用延遲
  const coldStartDelays = [5000, 10000, 15000]; // 5s, 10s, 15s
  retryDelay = coldStartDelays[retryCount] || 15000;

  errorMessage = language === 'zh-TW'
    ? '服務可能正在啟動中（首次訪問需要 30-60 秒），請稍候...'
    : 'Service may be starting up (first visit takes 30-60 seconds), please wait...';
}
```

#### 其他錯誤
- 使用指數退避（exponential backoff）
- 最多重試 3 次
- 顯示具體的錯誤訊息（timeout, 404, 500, offline）

## 命名慣例

### 檔案命名
- **React 元件**：PascalCase.tsx（`StockCard.tsx`, `TimeRangeSelector.tsx`）
- **工具函式**：camelCase.ts（`formatNumber.ts`, `dateUtils.ts`）
- **Python 模組**：snake_case.py（`stock_routes.py`, `stock_service.py`）

### 變數命名
- **React state**：camelCase（`stockData`, `isLoading`, `errorMessage`）
- **常數**：UPPER_SNAKE_CASE（`MAX_STOCKS`, `RETRY_DELAYS`）
- **Python 變數**：snake_case（`stock_data`, `retry_count`）

### 翻譯 Key 命名
- **camelCase**：`enterStockSymbol`, `maxStocksReached`, `failedToFetch`
- **分組邏輯**：header.*, stockManager.*, timeRange.*, chart.*

## 特殊注意事項

### Render Free Tier 限制
1. **閒置睡眠**：15 分鐘無活動後進入睡眠
2. **冷啟動時間**：30-60 秒
3. **前端處理**：已實作智能重試機制，自動處理冷啟動
4. **使用者體驗**：顯示友善的等待訊息

### 效能考量
- **API 快取**：5 分鐘快取減少 99.84% API 呼叫
- **惰性載入**：股票卡片按需載入資料
- **錯誤邊界**：單一股票錯誤不影響其他股票

### 資料限制
- **最多 18 檔股票**：`StockManager` 驗證邏輯
- **網格佈局**：6 行 x 3 列（最多 18 個卡片）
- **時間範圍**：最長 1 年歷史資料

## 開發工作流程

### 本地開發

#### 啟動前端
```bash
cd /Users/clementtang/stock-dashboard
npm run dev
# 運行在 http://localhost:5173
```

#### 啟動後端
```bash
cd /Users/clementtang/stock-dashboard/backend
source venv/bin/activate  # 啟動虛擬環境
PORT=5001 python app.py
# 運行在 http://localhost:5001
```

### 新增功能流程
1. 建立 TODO tracking（`.todo/` 資料夾）
2. 實作功能
3. 更新翻譯（如需要）
4. 更新 CHANGELOG.md
5. 更新 README（如影響使用方式）
6. 更新 package.json 版本號
7. Git commit（遵循 Conventional Commits）
8. Push 到 remote

### 測試檢查清單
由於目前沒有自動化測試，每次變更前請手動測試：

#### 前端測試
- [ ] 新增股票功能正常
- [ ] 移除股票功能正常
- [ ] 時間範圍切換正常
- [ ] 圖表顯示正確
- [ ] 雙語切換正常（zh-TW ↔ en-US）
- [ ] 主題切換正常（深色 ↔ 淺色）
- [ ] 顏色主題切換正常（Western ↔ Eastern）
- [ ] 錯誤處理正常顯示
- [ ] 重試機制正常運作

#### 後端測試
- [ ] API 端點正常回應
- [ ] 快取機制運作
- [ ] 錯誤處理正確
- [ ] CORS 設定正確

#### 跨瀏覽器測試（主要）
- [ ] Chrome/Edge
- [ ] Safari
- [ ] Firefox

## 部署注意事項

### Vercel 部署（前端）
- **自動部署**：Push 到 main 分支自動觸發
- **建構時間**：約 1-2 分鐘
- **環境變數**：在 Vercel Dashboard 設定 `VITE_API_URL`

### Render 部署（後端）
- **手動部署**：Dashboard → Manual Deploy
- **建構時間**：約 2-3 分鐘
- **健康檢查**：確保 `/api/health` 端點正常
- **地區**：Singapore（亞洲使用者低延遲）

### 部署後檢查
1. 檢查前端是否正常載入
2. 檢查 API 是否可以取得資料
3. 檢查錯誤處理是否正常
4. 檢查雙語功能是否正常

## TODO 追蹤系統

### 6 維度分類
MarketVue 使用 6 維度系統追蹤任務：

1. **Phase（階段）**：
   - P0（緊急）、P1（短期）、P2（中期）、P3（長期）

2. **Category（類別）**：
   - Feature、Enhancement、Bug Fix、Tech Debt、Documentation、Infrastructure

3. **Effort（工作量）**：
   - S（1-4h）、M（1-3d）、L（1-2w）、XL（2+w）

4. **Impact（影響）**：
   - High、Medium、Low

5. **Status（狀態）**：
   - 📋 Backlog、🎯 Planned、🚧 In Progress、✅ Done、❌ Cancelled

6. **Module（模組）**：
   - Frontend、Backend、Infrastructure、Docs、Full-stack

### TODO 檔案位置
- **任務清單**：`.todo/detailed-tasks.md`
- **每日工作記錄**：`.todo/work-logs/YYYY-MM-DD.md`
- **技術研究**：`.todo/research-reports/`
- **專案記錄**：`.todo/YYYYMMDD_feature_name.md`

### 工作記錄格式
每日工作記錄使用繁體中文，包含 12 個章節：
1. 📋 工作摘要
2. 🔍 問題診斷與排查
3. 🛠️ 問題解決
4. 💡 改進方案設計
5. 🚀 實施
6. 📊 成果統計
7. 🎯 技術亮點
8. 📝 學習與收穫
9. 🚀 部署狀態
10. ✅ 測試建議
11. 📅 時間分配
12. 🎉 總結

## 常見問題

### Q: 新增功能時忘記更新 CHANGELOG 怎麼辦？
**A**: 立即補上一個 commit：
```bash
git add CHANGELOG.md
git commit -m "docs: update CHANGELOG for v1.x.x"
git push
```

### Q: 如何處理 Render 服務睡眠問題？
**A**:
1. 前端已實作智能重試機制
2. 使用者會看到友善的等待訊息
3. 如需立即喚醒，可在 Render Dashboard 手動 Deploy

### Q: 如何新增對更多市場的支援？
**A**:
1. 確認 yfinance 支援該市場
2. 更新 `company_names.json` 對照表（如需要）
3. 更新 README 的支援市場列表
4. 測試股票代碼格式

### Q: 如何新增新的翻譯？
**A**: 參考本文件「雙語支援要求」章節的流程

## 發展藍圖

完整發展計畫請參考 `ROADMAP.md`，重點項目：

- **Q1 2025**（已完成）：
  - ✅ 18 檔股票支援
  - ✅ API 快取優化
  - ✅ 智能錯誤處理

- **Q2 2025**（規劃中）：
  - K 線圖
  - 新聞整合（Google News RSS）
  - CSV 匯出
  - 更多技術指標

- **Q3-Q4 2025**（未來）：
  - 投資組合追蹤
  - 使用者帳戶
  - 行動應用
  - AI 投資洞察

---

**參考其他指南**：
- `character.md` - 芙莉蓮角色設定和語氣風格
- `general-principles.md` - 通用開發原則（可複用）
- `workflows.md` - 具體協作流程和檢查清單
